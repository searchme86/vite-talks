import PrevButton from "./../../../Button/PrevButton/PrevButton";
import "./ChatTitle.style.scss";

function ChatTitle({ path, textTitle }) {
  return <PrevButton path={path} title={textTitle} />;
}

export default ChatTitle;
